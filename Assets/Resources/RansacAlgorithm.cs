using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Point
{
    double x;
    double y;
    double z;
}*/

public class RansacAlgorithm : MonoBehaviour
{
    /*
    Given:
    data – A set of observations.
    model – A model to explain observed data points.
    */
    
    
    void Start()
    {
        /*
        int i = 0, j = 0;
        double bestFit = 99999999999, bestErr = 99999, thisErr = 999999;
        int max_iteration = 9999999999999;
        Random r = new Random();
        bool found = false;
        point_count = 10; //number

        Point selected[] = new Point[3];
        double treshold = 0.001;

        for (i = 0; i < max_iteration && !found ; i++)
        {
            int randInt = r.Next(0, point_count);
            //maybeInliners -> noktalari depola
            //maybeModel -> fit olan noktalari burada depoluyoruz
            //alsoInliners -> suan bos set

            for (j = 0; j < 3; j++)
            {
                if ( treashold >  matchRate )
                {
                    //add point in alsoInliners
                }
            }

            if ( alsoInliners elemanları 3 olduysa gir ) //burda noktalari test ediyoruz 
            {
                
                betterModel:= model parameters fitted to all points in maybeInliers and alsoInliers
                thisErr := a measure of how well betterModel fits these points
                

                if (thisErr == 0)
                {
                    found = true;
                }

                if (thisErr < bestErr)
                {

                    //bestFit := betterModel
                    bestErr = thisErr;
                }
            }


        }*/

        //print best fit values
    }

}
